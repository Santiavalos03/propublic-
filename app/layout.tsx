import './globals.css'
export const metadata={title:'ProPublic Sistema Integral',description:'Gestión comercial y producción — ProPublic'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="es"><body>{children}</body></html>}
